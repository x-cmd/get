# shellcheck shell=bash

@src std/timer std/assert std/@

testcase(){
    timer.new ttt
    @defer "ttt.delete;"

    sleep 1
    assert.eq "$(ttt.elapsed)" 1
    ttt.reset
    assert.eq "$(ttt.elapsed)" 0
    sleep 1
    assert.eq "$(ttt.elapsed)" 1
}

# Temporary disable
# testcase
