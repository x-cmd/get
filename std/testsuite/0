# shellcheck shell=bash

@src(){
    while [ $# -gt 0 ]; do
        source "$(dirname "${BASH_SOURCE[0]}")/../../$1"
        shift
    done
}

find "$(dirname "${BASH_SOURCE[0]}")"/*.test | while read -r i; do
    #shellcheck disable=SC1090
    # ( source "$i" )
    source "$i"
done
