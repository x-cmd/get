# shellcheck shell=bash

@src std/file std/assert

test.file.to_array(){
    for i in $(seq 0 9); do
        echo "$i"
    done >./test.tmp
    @defer "rm $(pwd)/test.tmp"

    local vvv
    V=vvv file.to_array ./test.tmp
    assert.eq "${vvv[8]}" 8
}

test.file.to_array
